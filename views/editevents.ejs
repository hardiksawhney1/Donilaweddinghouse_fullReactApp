<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event</title>
    <style>
body {
  font-family: 'Arial', sans-serif;
  margin: 20px;
  background-color: #f8f8f8;
  color: #333;
}

h1 {
  color: #d32f2f; /* Dark red */
  text-align: center;
}

form {
  width: 50%;
  margin: 0 auto;
}

label {
  display: block;
  margin-bottom: 5px;
}

input[type="text"],
textarea,
input[type="date"] {
  width: calc(100% - 12px);
  padding: 8px;
  margin-bottom: 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

textarea {
  height: 100px;
  resize: vertical;
}

button[type="submit"] {
  padding: 8px 15px;
  background-color: #d32f2f; /* Dark red */
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

button[type="submit"]:hover {
  background-color: #b71c1c; /* Darker red on hover */
}
        </style>
</head>
<body>
    <h1>Edit Event</h1>
    <form action="/events/update/<%= event.id %>" method="post">
        <label for="venue">Venue:</label>
        <input type="text" name="venue" value="<%= event.venue %>" required />

        <label for="groom_name">Groom's Name:</label>
        <input type="text" name="groom_name" value="<%= event.groom_name %>" required />

        <label for="bride_name">Bride's Name:</label>
        <input type="text" name="bride_name" value="<%= event.bride_name %>" required />

        <label for="photo">Photo URL:</label>
        <input type="text" name="photo" value="<%= event.photo %>" required />

        <label for="noguests">Number of Guests:</label>
        <input type="number" name="noguests" value="<%= event.noguests %>" required />

        <label for="cost">Cost:</label>
        <input type="number" name="cost" value="<%= event.cost %>" required />

        <label for="description">Description:</label>
        <textarea name="description" required><%= event.description %></textarea>

        <button type="submit">Update Event</button>
    </form>
</body>
</html>
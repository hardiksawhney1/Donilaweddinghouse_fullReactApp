<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Review</title>
    <style>
body {
  font-family: 'Arial', sans-serif;
  margin: 20px;
  background-color: #f8f8f8;
  color: #333;
}

h1 {
  color: #d32f2f; /* Dark red */
  text-align: center;
}

form {
  width: 50%;
  margin: 0 auto;
}

label {
  display: block;
  margin-bottom: 5px;
}

input[type="text"],
textarea,
input[type="date"] {
  width: calc(100% - 12px);
  padding: 8px;
  margin-bottom: 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

textarea {
  height: 100px;
  resize: vertical;
}

button[type="submit"] {
  padding: 8px 15px;
  background-color: #d32f2f; /* Dark red */
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

button[type="submit"]:hover {
  background-color: #b71c1c; /* Darker red on hover */
}
    </style>
  </head>
  <body>
    <h1>Edit Review</h1>
    <form action="/reviews/update/<%= review.id %>" method="post">
      <label for="username">Username:</label>
      <input type="text" name="username" value="<%= review.username %>" required />

      <label for="photo_url">Photo URL:</label>
      <input
        type="text"
        name="photo_url"
        value="<%= review.photo_url %>"
        required
      />

      <label for="groom_name">Groom Name:</label>
      <input type="text" name="groom_name" value="<%= review.groom_name %>" required />

      <label for="bride_name">Bride Name:</label>
      <input type="text" name="bride_name" value="<%= review.bride_name %>" required />

      <label for="review">Review:</label>
      <textarea name="review" required><%= review.review %></textarea>

      <!-- <label for="date">Date:</label>
      <input type="date" name="date" value="<%= review.date %>" required /> -->

      <button type="submit">Update Review</button>
    </form>
  </body>
</html>
